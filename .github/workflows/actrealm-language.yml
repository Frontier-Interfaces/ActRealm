name: ActRealm language guard

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  language:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5

      - name: Validate product and Runtime language contracts
        run: ./scripts/check-actrealm-language.sh
