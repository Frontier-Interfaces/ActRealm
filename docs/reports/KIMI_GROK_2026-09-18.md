# Kimi / Grok local candidate verification

ActRealm 0.1.0 build 126 was installed on 2026-09-18 from the modified
`codex/kimi-grok-providers` worktree at base `bf29aee`. The package used uncommitted source at validation time.
This is local candidate validation, not release approval.

## Installed behavior

- Agent Setup exposes Claude, Codex, Kimi and Grok. Grok has verified real
  events; Kimi remains awaiting real-session verification.
- Ordinary CLI Hooks observe events. The copied connected-session command
  provides a live ACP reply channel for supported approvals and questions.
- The token summary displays four provider chips in two columns. Grok had
  approximately 230,000 observed tokens during inspection; Kimi showed awaiting
  collection. The dashboard includes Grok in its provider breakdown.
- Usage-source localization covers `grok_session_usage` and `agent_connector`.
  Available data retains explicit partial-history coverage.
- Missing account quota is unavailable, not a zero balance or fabricated
  percentage.

## Real-provider evidence

On 2026-09-17, Grok Build 1.0.34 completed a structured question, consumed the
answer Blue, requested a file approval, entered the three-second pending commit
window, and wrote Blue to the test file. Two independent isolated Runtime
restarts both reported 47,544 tokens for that session.

A separate installed-native-UI test answered the Grok question with 通过 and
verified that exact text in the resulting file. That second run did not request
another file approval under the provider's policy; the approval evidence comes
from the earlier isolated test. An ordinary CLI session also appeared with its
real token metrics.

Kimi Code 2.0.0 login and ACP initialization succeeded. A real prompt returned
an exhausted monthly allowance. The user deferred further real Kimi model
tests, and this continuation made none. Kimi's complete live-model workflow
therefore remains unaccepted. Automated protocol coverage does not replace it.

Live Grok denial, undo, cancellation and resume were not separately re-exercised
for build 126. Protocol/lifecycle tests cover the implemented paths; this report
does not claim broader real-provider acceptance than the evidence above.

## Automated verification

- Rust workspace: 423 passed, zero failed, three ignored.
- Clippy: workspace/all-targets, offline, warnings denied, passed.
- Rust release build and formatting checks passed.
- UTC macOS suite: 213 tests in 31 suites passed.
- Language checks: 64 Runtime messages, 81 API errors, 61 emitted message codes,
  and 1,895 English entries passed.
- Info.plist validation, release packaging, deep strict signature verification
  and `git diff --check` passed.

Final test/package logs were recovered from the interrupted task. Source
timestamps confirmed no Rust edits after its final workspace tests, and no
Swift edits after its final tests or package. The continuation rechecked
formatting, metadata, installed artifacts and native UI.

## Artifact and service checks

The installed build 126 executable SHA-256 is
`22e89d04c0ff3ca07763cca5401ca9763cda099d577755226d0cf55742058295`.

Build 126 changes the UI only relative to build 125. The packaged Runtime was
compared with the running helper after removing signatures from temporary
copies, proving equivalent code. The existing signed helper was retained in
the final app and the outer bundle was signed again. Bundled and shared helper
SHA-256 remained
`0391017c3bb50babaed9275979fa9f172e51ffddfdeddb8a52cb70453a9a616c`.
The existing Runtime process stayed running and the new native app reconnected.

Display 0.39.4 build 86 was unchanged; its executable SHA-256 remained
`fddfe28ea21aeb4da2819d3dcf0625775ae0690e00ea631698de604aa845b3be`.
Its worktree remained clean. Deferred Display work is retained separately as a
patch. The replaced ActRealm app was moved to Trash with a recovery journal.
